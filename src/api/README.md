# API Layer

Contains the REST API implementation for the chatbot.

## Structure

- `routes/` - API route definitions
- `controllers/` - Request/response handling logic  
- `middleware/` - Authentication, validation, and other middleware
